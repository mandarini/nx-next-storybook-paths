import type { Meta } from '@storybook/react';
import { ReactLibNone } from '@path-imports/react-lib-none';

const Story: Meta<typeof ReactLibNone> = {
  component: ReactLibNone,
  title: 'ReactLibNone',
};
export default Story;

export const Primary = {
  args: {},
};
